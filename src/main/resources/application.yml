spring:
  application:
    name: streamingFlix

  datasource:
    url: jdbc:postgresql://localhost:5432/streamingflixdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true